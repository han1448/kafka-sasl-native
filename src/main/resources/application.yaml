spring:
  application:
    name: kafka-sasl

  # Kafka properties
  kafka:
    security:
      protocol: "SASL_PLAINTEXT"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      bootstrap-servers: localhost:9092
      properties:
        sasl:
          jaas:
            config: "org.apache.kafka.common.security.plain.PlainLoginModule required username=\"admin1\" password=\"admin-secret\";"
          mechanism: "PLAIN"
